# Resumo
Esse projeto foi desenvolvido por meio de um turorial que visava fazer um clone melhorado da calculadora que foi lançada recentemente

# Start Front-End
 - ```cd calculator-frontend ```
 -  ```npm install```
 -  ```npm create vite@latest```
 -   ```npm run dev```

# Front End
- ```cd <nomedoprojeto-frontend>```
- ```npm install -D tailwindcss post css autoprefixer```
- ```npx tailwindcss init -p```
- ```npx shadcn@latest init```
- ```npm i @types/node```
- ```npx shadcn@latest add button```
- ```npm i react-router-dom```
- ```npm install @mantine/core @mantine/hooks```
- ```npm install --save-dev postcss postcss-preset-mantine postcss-simple-vars```

# Back End
- ```python -m venv venv```
- ```venv\scripts\activate```
- ```python main.py```  

